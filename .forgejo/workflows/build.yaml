concurrency:
  cancel-in-progress: true
  group: ${{ forge.ref }}-${{ forge.workflow }}

jobs:
  build:
    container:
      image: git.zarantonello.dev/projects/forgejo-agent-ci:v2026.08.27.1@sha256:b16d47dda5992a423df060f4101f3eb71b8bc4cdb81867e68e749b466a774e3e
      volumes:
        - forgejo-runner-cache-pnpm:/usr/local/pnpm/store
    name: Build the executable
    runs-on: srv-hetz-nbg-0
    steps:
      - name: Clone the repository
        run: |
          git clone --filter=blob:none --no-checkout https://${{ forge.token }}@git.zarantonello.dev/${{ forge.repository }} ${{ forge.workspace }};
          git checkout --quiet ${{ forge.sha }};
      - name: Install dependencies with pnpm
        run: pnpm install
      - name: Build
        run: pnpm run build
      - name: Upload the package
        run: |
          curl -s \
            -H "Authorization: Bearer ${{ secrets.CI_BOT_REGISTRY_TOKEN }}" \
            --upload-file dist/forgejo-agent \
            https://git.zarantonello.dev/api/packages/projects/generic/forgejo-agent/${{ forge.ref_name }}/forgejo-agent

name: build.yaml

"on":
  push:
    tags:
      - v*
